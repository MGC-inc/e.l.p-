# 引き継ぎ：組織ゴールマップ開発（新規セッション用）

> このファイルは、ゴールマップ／組織ロードマップ開発セッションの続きを**新しいセッションで再開するための引き継ぎメモ**。新セッションを開いたら、このファイルの内容を踏まえて会話を再開してほしい。

- **リポジトリ**: `MGC-inc/e.l.p-`
- **作業ブランチ**: `claude/zen-hamilton-uwck7r`（2026-07-08時点、tree clean・push済み）
- **最新コミットの確認**: `git log --oneline -5`（このファイル自体の更新日以降にも作業が進んでいる可能性があるため、固定ハッシュではなくこのコマンドで都度確認する）

## 0. 新規セッションの始め方

| 項目 | 指定内容 |
|---|---|
| リポジトリ | `MGC-inc/e.l.p-` |
| ブランチ | `claude/zen-hamilton-uwck7r`（**`main`ではない**。指定できない場合は最初のメッセージで `git fetch origin claude/zen-hamilton-uwck7r && git checkout claude/zen-hamilton-uwck7r`） |
| 実行場所 | リポジトリルート。組織ゴールマップ開発時のみ `tools/roadmap/app/` へ`cd` |
| モード | 通常モード。Notion新DB設計など大きな設計判断をゼロから詰める回のみプランモード |
| モデル | 通常は **Sonnet 5**。Notionスキーマ設計等、曖昧さが大きい判断が必要な回は **Opus 4.8** |

**コピペ用プロンプト（汎用・まず状況把握させる）**：
```
このリポジトリ（MGC-inc/e.l.p-）のブランチ claude/zen-hamilton-uwck7r で
ゴールマップ開発を引き継ぎます。今のブランチがこれになっているか確認し、
違えば git fetch origin claude/zen-hamilton-uwck7r && git checkout claude/zen-hamilton-uwck7r
してください。

まず 引き継ぎ_組織ゴールマップ.md をリポジトリルートから読み込み、
現在の実装状況・決定事項・Notion権限の状態・次にやるべきこと（Phase B/C）を
把握してください。読み込んだら要点を短く報告し、次に何をするか私の指示を待ってください。
```

**目的別バリエーション**：
- **Phase B着手**：「引き継ぎ_組織ゴールマップ.md を読み込んでから、tools/roadmap/README.md の『Phase B』に沿ってNotion新DBを設計してください。実装前に会社の実目標・組織名・チーム名を私にヒアリングしてから進めてください」
- **メンバー追加/削除**：「引き継ぎ_組織ゴールマップ.md の『メンバー一覧のメンテナンス手順』どおりに〈名前〉を〈追加/削除〉してください」
- **動作確認/デプロイ**：「引き継ぎ_組織ゴールマップ.md を読み込んでから tools/roadmap/app/ で `npm install && npm run dev` して動作確認し、Vercelへのデプロイも試みてください（コネクタ未認可なら報告してください）」

## 1. 全体像（今どういうシステムがあるか）

```
tools/goalmap/goalmap-studio.html   … 個人ゴールマップ（オフライン単体HTML・完成度高）
        ↑ ?member=名前 で自動選択
tools/roadmap/app/                  … 組織ゴールマップ（会社→組織→チーム→個人・React Flow・Phase A完了）
        ↓ 個人ノードclickで上に遷移
Notion（🧭目標マップ／✅GMタスクDB）  … 個人データの正（10メンバー分・実データ反映済み）
```

- **個人ゴールマップ**（`tools/goalmap/`）＝週次MTGで使う、ビルド不要・ダブルクリックで開けるオフラインツール。**これは完成しており、今後も単体配布を維持する。**
- **組織ゴールマップ**（`tools/roadmap/`）＝今回新規追加。会社→組織→チーム→個人をGoogleマップのようにズーム（クリック展開）して辿れるビュー。**このリポジトリ初のビルド環境**（npm/Vite/React/TypeScript/`@xyflow/react`）。Phase A（土台）のみ完了、Phase B（Notion連携・実データ・デプロイ）は未着手。

## 2. 個人ゴールマップ（`tools/goalmap/goalmap-studio.html`）の状態

### 実装済み機能
- **データモデル**：`name/note/theme/goal/vision/visionDue/midGoal/midDue/why/focus/next/currentStage/cycle/logs/startDate/dueDate/phases[5]`
- **成長キャラ**：卵→ひよこ→…→不死鳥（1タスク=餌）。ゴール達成→次の周回で`cycle`を胸に刻印（何羽目か分かる）
- **目標の3段階層**：大枠ゴール（`vision`・長期北極星）＞中目標（`midGoal`・中期）＞今期ゴール（`goal`）。地図上部に積み上げ表示、「その先へ」矢印で連結
- **🎙 MTログ/議事録**：ブラウザ内録音（MediaRecorder）、議事録メモの蓄積・Markdown書き出し、次アクション→タスク化ボタン
- **🔒 履歴を見る**：過去周回の読み取り専用プレビュー（localStorage非破壊）
- **連続ズーム＋タップドリルダウン**：スクロール/ピンチ/ドラッグで地図を連続的にpan/zoom。大枠/中目標/今期ゴール/各フェーズをタップすると、その中の詳細（タスク）へ滑らかに寄っていく演出。ヘッダーの🔭セグメント（🏠現在地/🗾今期/🌏中目標/🌌大枠）でプリセットジャンプも可能
- **文字はみ出し対策**：タスク名の自動折り返し・枠の可変高さ・長いタイトルの自動縮小
- **`?member=名前`クエリ対応**（今回追加）：組織ゴールマップからの遷移時に対象メンバーを自動選択

### データの状態
- 対象10名：三浦・今川・岡野・岡野（育成）・宮腰・戸田・藤江・藤江（育成）・門田・古賀
- **Notion🧭目標マップ＋✅GMタスクDBの本番データを反映済み**（2026-07-08）。ゴール・テーマ・現在ステージ・タスク（68件）とも実データ。古い/サンプルのNotion行（54件のタスク）は除外済み
- 岡野・藤江は「クローザー」と「育成」の2ゴールを別マップとして保持（本人が複数の実ゴールを持っていたため）

### Notion権限の状態（重要）
- `.claude/settings.local.json` に **Notion読み取り系ツールのみ**自動許可を設定済み（`notion-query-data-sources`, `notion-fetch`, `notion-search` 等）
- **書き込み系**（`notion-create-pages`, `notion-update-page` 等の一括自動許可）は、Claude Codeの自動判定（セルフ変更の安全弁）にブロックされ**未設定**。Notionへの書き込みを自動化したい場合は、ユーザーに明示的な許可を再度取るか、ユーザー自身が設定ファイルに追記する必要がある

### 既知の未解決事項
- 岡野の実タスク数がリポジトリとNotionで一部食い違っている可能性（セッション序盤で発見、深掘りせず保留）

### メンバー一覧のメンテナンス手順（追加・削除）

ゴールマップに関わる「メンバー一覧」は目的別に複数箇所に分散している。**まず「実データを持つ人の話か、候補リストだけの話か」を区別する**（下記A/B/C）。

#### A. 「今後ゴールマップを作る予定」の候補リストだけを変える（実データはまだ無い）

コード変更は不要。2ファイルをテキスト編集するだけ：
- `ゴールマップ運用.md` の `## 4. Notion構造` 内「対象メンバー（N名）：」の行
- `tools/goalmap/notion_setup_prompt.md` の「**メンバー**（セレクト：…）」の行

編集後、リポジトリルートで:
```bash
git add ゴールマップ運用.md tools/goalmap/notion_setup_prompt.md
git commit -m "docs(goalmap): 候補メンバー一覧を更新"
git push
```
（2026-07-08：この手順で山下・鈴木を削除した。実データ`members/*.json`には元々含まれていなかったため、これで完結した。）

#### B. 実際にゴールマップを持つ人を「追加」する

```bash
cd tools/goalmap

# ① テンプレートから新規JSONを作成し、中身を埋める
cp members/_template.json "members/<名前>.json"
# name/note/theme/goal/vision/visionDue/midGoal/midDue/why/focus/next/
# currentStage/cycle/phases[].tasks/startDate/dueDate を編集

# ② studio内蔵のEMBEDDED配列を、members/*.json全件から自動再生成
python3 - <<'PY'
import json, re, glob, os

order = sorted(
    os.path.splitext(os.path.basename(p))[0]
    for p in glob.glob("members/*.json")
    if not os.path.basename(p).startswith("_")
)
lines = ["  " + json.dumps(json.load(open(f"members/{n}.json", encoding="utf-8")), ensure_ascii=False) for n in order]
block = "const EMBEDDED = [\n" + ",\n".join(lines) + "\n];"

html = open("goalmap-studio.html", encoding="utf-8").read()
new = re.sub(r"const EMBEDDED = \[.*?\n\];", block, html, count=1, flags=re.S)
assert new != html, "置換が発生しませんでした（EMBEDDED定義の書式が変わっていないか確認）"
open("goalmap-studio.html", "w", encoding="utf-8").write(new)
print("EMBEDDED regenerated:", order)
PY

# ③ JS構文チェック（<script>タグの中身を抜き出してnodeで検証）
python3 -c "
import re
html=open('goalmap-studio.html',encoding='utf-8').read()
m=re.search(r'<script>(.*)</script>',html,re.S)
open('/tmp/gm_check.js','w',encoding='utf-8').write(m.group(1))
"
node --check /tmp/gm_check.js && echo JS_OK

# ④ 図解PNG・1枚カードを再生成
python3 generate_goalmap.py "members/<名前>.json"
python3 summary_cards.py "<名前>"

# ⑤ ドキュメント更新（人数・名前を反映）
#    ゴールマップ運用.md の「対象メンバー（N名）：」の行を編集
```

コミット：
```bash
cd /home/user/e.l.p-
git add tools/goalmap/members/<名前>.json tools/goalmap/goalmap-studio.html \
        tools/goalmap/out/<名前>.* tools/goalmap/out/summary/<名前>.* ゴールマップ運用.md
git commit -m "feat(goalmap): <名前>のゴールマップを追加"
git push
```

#### C. 実際にゴールマップを持つ人を「削除」する

```bash
cd tools/goalmap
rm "members/<名前>.json"
rm -f "out/<名前>.svg" "out/<名前>.png" "out/summary/<名前>.svg" "out/summary/<名前>.png"
# ↑ 削除後、Bの②③（EMBEDDED再生成＋JS構文チェック）を同じスクリプトでそのまま実行
#   ＝glob(members/*.json)が自動的に削除済みメンバーを除外するので、スクリプトの変更は不要
```
その後、`ゴールマップ運用.md`から名前を削除してコミット。

#### D. その他のメンバー一覧

| 変えたいこと | 触る場所 |
|---|---|
| 組織ゴールマップ（React版）のチーム所属 | `tools/roadmap/app/public/data/roadmap.json` を直接編集（現状サンプルデータ。Phase B後はNotion同期で自動生成予定） |
| Notionの目標マップ/GMタスクDBのメンバー選択肢 | Notion UI側で直接削除（selectオプションは新規ページ作成時に自動追加されるため、データ本体には影響しない） |

> ⚠️ 従業員名簿（`従業員.md`）は実在の社員情報なので、ゴールマップの対象外になっても**削除しない**（完全に別管理）。

## 3. 組織ゴールマップ（`tools/roadmap/app/`）の状態 — Phase A完了・Phase B項目1〜4完了（5デプロイのみ残）

### 決定事項（ユーザー確認済み）
1. **技術基盤**：React Flowで本格実装（ビルド必須。個人ゴールマップのoffline単体HTML配布とは別立て）
2. **Level3（個人）の中身**：既存`goalmap-studio.html`をそのまま活かす（作り直さない）。個人ノードclick→`goalmap-studio.html?member=<名前>`へ遷移
3. **Level0〜2（会社/組織/チーム）データ**：Notionに新DB「🏢 組織ロードマップ」を作成済み（2026-07-09）

### 実データ確定事項（2026-07-09ヒアリング）
- **会社KGI**：売上30億円（代理店30社×平均年商1億円）、期限2029-03-31
- **組織（Level1）**：営業部／施工部（マーケティング部は実在しないため対象外。管理部は2026-07-09に廃止し営業部へ統合）
- **チーム（Level2）**：
  - 営業部＞ピタサチ（三浦・藤江・藤江育成、責任者：門田）
  - 営業部＞wanny（岡野・岡野育成・宮腰・戸田・古賀、責任者：岡野）
  - 施工部＞関東／中部／関西（施工部は実在するがチーム編成・メンバーは未確認の暫定値。**要確認**）
- **2026-07-09の組織変更**：管理部／マネジメントチームを廃止し、営業部に統合。今川は代理店チームを持たないため営業部（Level1）に個人ノードとして直接接続（`所属チーム`リレーションはLevel2チームだけでなくLevel1組織も接続先にできるよう`sync_notion_to_json.py`を拡張済み）。岡野・門田はそれぞれwanny/ピタサチチームの1ノードのみ（マネジメント業務は分離せず同じノードのタスクに混在させる方針）

### Phase Aで実装したもの
- `tools/roadmap/app/`：Vite + React + TypeScript + `@xyflow/react`
- 汎用ノード型 `RoadmapNode`（`src/types/roadmap.ts`）：`title/description/progress/deadline/owner/priority/status/children`、`level`で0〜5（会社/組織/チーム/個人/タスク/サブタスク）を区別。`children`で無限階層化可能
- 独自ツリーレイアウト（`src/lib/tree.ts`、外部レイアウトライブラリ不使用）＋クリックで展開・自動`fitView`（`src/components/RoadmapCanvas.tsx`）
- 表示ルール実装済み：初期は会社ノードのみ（部署は折りたたみ）。クリックで子が展開、兄弟ノードは畳まれたまま。もう一度クリックで折りたたみ
- パンくず（左上）＋HUD（左下・現在地とズーム%）で「今どこにいるか」常時表示
- 個人（Level3）ノードclick→`goalmap-studio.html?member=X`へ遷移（studio側は上記の`?member=`対応で自動選択）
- ビルド時に`tools/goalmap/goalmap-studio.html`を`public/goalmap-studio.html`へ自動コピー（`scripts/copy-studio.mjs`）。正データは`tools/goalmap/`のまま、二重管理を回避
- データソース：`public/data/roadmap.json`（起動時fetch）。**実データに更新済み**（2026-07-09。上記「実データ確定事項」参照。`src/data/roadmap.sample.json`＝ビルド同梱フォールバックも同期済み）
- 検証済み：`npm run build`成功（TypeScriptエラーなし）、Playwrightで4段階ドリルダウン・パンくず・個人ノードからのstudio遷移・自動選択・コンソールエラー0を確認（Phase A時点。Phase Bのデータ更新後の再確認は未実施）

### 起動方法
```bash
cd tools/roadmap/app
npm install
npm run dev       # http://localhost:5173
# または
npm run build && npm run preview
```

## 4. 未着手（Phase B / C）— 次にやるべきこと

**Phase B（組織ロードマップの本実装）**：
1. ✅ Notionに新DB「🏢 組織ロードマップ」を作成（名称／レベル／説明／進捗率／期限／責任者／優先度／ステータス／親・子（自己リレーション）。data_source_id=`8a0f76d4-2eeb-40ce-8209-2ccc3179bcb8`、database_id=`d6bd4fd0-7d41-4c2e-9862-ef43527dbe45`。「🧭 個人ゴールマップ」ページ直下に配置）
2. ✅ 既存🧭目標マップDBに「所属チーム」リレーションを追加し、Level2→Level3を接続（実データ15行に設定済み。兼務者は複数チーム選択）
3. ✅ 実際の会社目標・組織名・チーム名をヒアリング済み（`public/data/roadmap.json`を実データに更新・push済み）
4. ✅ `tools/roadmap/sync_notion_to_json.py`を作成し、ユーザー提供の`NOTION_TOKEN`で実行確認済み（2026-07-09）。`public/data/roadmap.json`・`src/data/roadmap.sample.json`はこのスクリプトが生成したデータに更新済み・push済み。`npm run build`成功も確認
   - 重要な仕様：チームごとにメンバー名で重複除去し、1人1リーフが基本。ただし`tools/goalmap/members/*.json`に「〇〇（育成）」のような同一人物の複数ファイルが実在する場合（岡野・藤江）は、その数だけリーフを分ける（Notionの「役割」列は当てにならず使っていない）
   - 既知の制約：🧭目標マップDBの「進捗率」ロールアップが全行`null`を返す（Notion側の集計設定の問題と思われる、本セッションが作った問題ではない）。そのためLevel3個人ノードの`progress`は現状すべて未設定
5. ⬜ Vercelへデプロイ（**Vercelコネクタの認可がユーザー側で必要** — claude.aiのコネクタ設定から。非対話セッションでは認可不可）

**Phase Bの残タスク**：
- 施工部の関東／中部／関西というチーム編成は未確認の暫定値。実際のチーム構成が分かり次第、Notion側（🏢組織ロードマップDB）と`roadmap.json`の両方を修正する
- 🧭目標マップDBの「進捗率」ロールアップが`null`になる問題の原因調査（Notion側のロールアップ設定）
- データ更新後、`npm run dev`でのPlaywright再確認（Phase Aで実施した4段階ドリルダウン等の回帰チェック）

**Phase C（将来・任意）**：
- Level4（タスク）/Level5（サブタスク）専用UI（現状は個人ゴールマップ内のフェーズ/タスクで代替）
- ライブNotion API同期（Vercel Serverless Function＋環境変数トークン。現状は静的JSONのオンデマンド同期のみ、秘密情報をクライアントに埋め込まない設計）

**PRについて**：
- PR #2（個人ゴールマップの初期実装〜Notion反映まで）は**マージ済み**
- それ以降のコミット（タップドリルダウン、Notion本番データ反映、組織ゴールマップPhase A）は**まだPRになっていない**。区切りがついたら新規PRを作成する（ユーザーの指示があれば）

## 5. 開発時の注意点（このリポジトリ特有のルール）

- `CLAUDE.md`のルール厳守：シークレット値をリポジトリに書かない、機微情報（住所・電話番号・給与）を書かない、日付は`YYYY-MM-DD`、Conventional Commits、新規ファイル追加時は`E.L.P.md`のドキュメントマップに行を追加
- コミットのフッターに `Co-Authored-By` と `Claude-Session` を必ず付与（既存コミット参照）
- Notion MCPツールは`.claude/settings.local.json`で読み取りのみ自動許可済み（§2参照）
- `tools/roadmap/app/`は`node_modules`・`dist`・`*.tsbuildinfo`・自動コピーされる`public/goalmap-studio.html`を`.gitignore`済み

## 6. 関連ドキュメント
- 個人ゴールマップの仕様: [`tools/goalmap/README.md`](./tools/goalmap/README.md)
- 週次運用ルール: [`ゴールマップ運用.md`](./ゴールマップ運用.md)
- 組織ゴールマップの仕様・Phase B/C詳細: [`tools/roadmap/README.md`](./tools/roadmap/README.md)

# ゴールマップ運用の見直し検討（ドラフト）

> **位置づけ**: このファイルはドラフト（検討中）です。`ゴールマップ運用.md`（確定版）はまだ書き換えていません。下記の論点について指示をもらってから、確定版に反映します。`E.L.P.md`のドキュメントマップにもまだ追加していません。

## 1. 現状の運用（要約）

`ゴールマップ運用.md`に「確定」として明文化されている現行ルール：

| 項目 | 現状 |
|---|---|
| 編集の本拠地 | `goalmap-studio.html`（週1・MT中にまとめて編集） |
| データの正 | studio（ブラウザlocalStorage＋`members/<名前>.json`） |
| LINE | **人力共有のみ**。PNGを書き出してLINEアプリで手動送信。bot/API連携は無し |
| Notion | **蓄積・参照用**（主にスマホ閲覧）。週次では触らない方針。随時・節目でのみ「Notionに反映して」と指示して更新 |
| 📅日報機能 | studioに実装済み（毎日の商談内容・気づき・共有事項を記録）だが、**実データでは誰も使っておらず事実上未稼働**（`members/*.json`に`daily`キーを持つファイルが1つも無い） |
| 個別リンク | `?member=名前`で直接開ける。**加えて今回のセッションで「この端末で最後に見ていたメンバーを自動復元」を実装済み**（`?member=`無しで開いても前回見ていた人のページが開く） |
| 達成率 | タスクの`done:true`件数 ÷ 全タスク数 ×100（自動計算） |
| 成長レベル（卵→龍） | **人間（メンター）が週次MTGで判断して上げる**。タスク完了は「ゲージ（餌）」止まりで、レベルを自動では上げない設計（見た目だけの水増しを防ぐため） |
| ゴール逆算構造 | 大枠ゴール▸中目標▸今期ゴール▸5フェーズ（①型を知る→②練習→③実践→④振り返り→⑤自走）▸タスク、という逆算構造が既に実装済み |

## 2. ご提示の新フロー（要約）

**■ 日常の運用**
1. LINEで毎日報告を上げる
2. それをノート（Notion）にまとめる
3. 今日の反省点・気づき・学びを、本人が学んだ一つの出来事として週単位でまとめる

**■ 週次ミーティングと振り返り**
1. 週次MTGで各自振り返る
2. 改善できているか・意識できているかをチェック
3. 各自が共有した内容を、個別リンクから振り返る。または同じリンクにログインして各自のページを見られるようにする
4. 今週共有した学び・気づきを、週1回振り返ってチェックを入れる
5. チェック項目が増えるにつれて成長を実感できる

**■ 大前提**: 「ゴールマップ」なので、ゴールから逆算して「今何をしなければいけないか」が見える状態が理想

## 3. 既存機能でそのまま対応できる点

新フローのうち、**追加開発なしで既に対応済み**のもの：

- ✅ 「個別リンクから振り返る／同じリンクで各自のページを見る」→ `?member=`個別リンク＋最後に見ていたページの自動復元（実装済み）
- ✅ 「ゴールから逆算して今何をすべきか見える」→ 大枠▸中目標▸今期ゴール▸5フェーズ▸タスクの逆算構造（既存の中核思想）
- ✅ 「チェックが増えるにつれ成長を実感」→ 達成率％の自動計算＋成長レベル（卵→龍）の仕組み（既存）

## 4. 矛盾・要検討ポイント（5つ）

### a. Notionの位置づけ
- **現状**: 「週次では触らない、蓄積・参照専用」と明記
- **新フローの意図**: 「毎日LINE→Notionにまとめる」が日常運用の起点になっている
- **論点**: Notionを「毎日更新する場所」に格上げするか？　それとも従来通りNotionは参照専用のままにして、LINE報告は別の置き場所（例：studioの日報欄）に集約するか？

### b. LINE入力の取り込み方法
- **現状**: LINE連携は皆無（bot/webhook/APIコードなし）。共有は人力のみ
- **新フローの意図**: LINEでの毎日報告が運用の起点
- **論点**: 当面は「本人がLINEに書いた内容を、後で自分でNotionやstudioに手動転記する」运用でよいか？　それとも自動連携（LINE Bot等）を今後作るか（範囲・コストが大きく変わる）？

### c. 「週1つの学び」の置き場所
- **現状**: 📅日報＝毎日複数件蓄積、🎙MTログ＝週次の議事録（別物）
- **新フローの意図**: 「本人が学んだ一つの出来事」を**週単位で1件**にまとめる、という新しい粒度
- **論点**: 既存の日報欄をそのまま「週1件だけ書く」運用にするか、MTログの一部として書くか、それとも新しい項目を追加するか？

### d. 「学び・気づきの振り返りチェック」と既存の達成率の関係
- **現状**: 達成率＝タスク完了率のみで計算。日報・学びの記録は達成率に一切影響しない
- **新フローの意図**: 「学び・気づきにチェックを入れる」ことが成長実感の源泉になっている
- **論点**: この「学びのチェック」は今の達成率計算に組み込むか、独立した別のチェックリストとして持つか？

### e. 「ログイン」という表現の実態
- **現状**: 認証なしの共有URL（`?member=`）＋端末ごとのlocalStorageによる自動復元のみ
- **新フローの意図**: 「同じリンクにログインした上で各自のページを見られるように」という記述
- **論点**: 本当の意味での個人認証（ID/パスワードなど）が必要か？　それとも現状の「共有リンク＋その端末での自動復元」の仕組みで意図は満たせているか？（認証を追加すると開発規模が大きく変わる）

## 5. たたき台案（議論のスタート地点として1つだけ提示）

既存の日報欄・個別リンク機構を最小改修で転用する案：

1. 本人はLINEに毎日書いたことを、週の終わりに自分で1件だけ選び、studioの📅日報欄（または新設する「今週の学び」欄）に転記する
2. Notionは現状通り参照専用のまま維持し、格上げしない（節目で反映）
3. 週次MTGでは、個別リンク（現状のまま・ログイン機能は追加しない）を開き、今週の学び1件を振り返りながら、既存のタスクチェック・フェーズ調整を行う
4. 「学びのチェック」は達成率計算には含めず、日報欄に「振り返り済み」フラグを立てるだけの軽い仕組みとして別管理にする

※これはあくまで叩き台であり、上記a〜eの論点についてご判断いただいた上で確定します。

## 6. 次のアクション

1. 上記4節の a〜e、5つの論点について回答・指示をください
2. 回答を踏まえて、具体的な修正（studioへの機能追加、`ゴールマップ運用.md`本体の更新、必要なら`E.L.P.md`のドキュメントマップ追記）に着手します

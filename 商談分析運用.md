# 商談分析運用.md — 録音→商談分析→PDCAの回し方

> 商談の録音を「聞いて終わり」にせず、**分析 → 個人の改善アクション → 次の商談 → 週次レビュー** のPDCAサイクルに変えるための運用ファイル。
> 個別の分析結果は **Notion「DB 商談分析＆アポ分析」** に蓄積する（クローザー別/アポインター別契約率などのダッシュボードが既に稼働中）。このリポジトリでは、そこに至るまでの取り込み・分析パイプラインと、週次のチームPDCAを扱う。

## 0. LINEアカウント構成

| アカウント/場所 | 用途 | 対象 |
|---|---|---|
| **@124rnagj（既存・変更なし）** | タスク通知・日報リマインドなど、今まで通り | 社内メンバーのみ |
| **ユメイク営業分析bot（新規作成）** | 商談録音の受付・アポインター/お客様名/結果の聞き返し・分析トリガー | 社内クローザー＋代理店、**営業に関わる人を1つに一本化** |
| **基準値報告グループ（既存の社内グループ、変更なし）** | 訪問/在宅/対面/対象/対話/アポ/商談/契約/保留/失注/クーリングオフ/審査落ちの日次数字（今川さんが手動でNotionに反映） | 社内限定 |

- 「ユメイク営業分析bot」は@124rnagjとは**別アカウント**として新規作成する。社内・代理店を問わず、営業に関わる人はこのBotを友だち追加して1対1でやり取りする
- 基準値報告グループは今まで通り社内限定のまま維持する（代理店の人は入らない）。将来的に「ユメイク営業分析bot」をこのグループにも招待し、日次サマリーの自動投稿やゴールマップ最新版へのリンク共有、チームからの質問への回答（例:「今月の契約率は？」）などに拡張できる余地を残しておく（**今回のスコープ外**。まずは商談分析を仕上げてから着手する）
- 1つのLINEアカウントは「個人との1対1」と「グループへの参加・投稿」を両方こなせる。1対1でのやり取り（ユメイク営業分析bot経由）とグループでのやり取り（基準値報告グループ）は別々の"部屋"なので、代理店の人にグループの内容が見えることはない

### 「ユメイク営業分析bot」新規作成時のチェックリスト

- [x] LINE Official Account Managerで新しい公式アカウントを開設する（アカウント名: 「ユメイク営業分析bot」、アイコン設定済み）
- [x] Messaging APIを有効化する（プロバイダー: 既存の「ユメイクLINE」を選択）
- [x] 「応答設定」を確認する（Webhookは受信プログラム実装前のため意図的に**OFF**のまま。あいさつメッセージもOFF。実装着手時にWebhook URLを設定してからONにする）
- [x] 「グループ・複数人トークへの参加を許可」を **ON** にする
- [x] チャネルシークレット・チャネルアクセストークンを発行する（値は今川さんが安全な場所（パスワード管理ツール等）に保管済み。実行先は`line-webhook/`（今川さん個人のVercelアカウント）の環境変数に設定する）
- [ ] `line-webhook/README.md`の手順でSupabase・Vercelをセットアップし、Webhook URLをLINE側に設定してWebhookを**ON**にする
- [ ] あいさつメッセージの文面を設定する（本ファイル セクション3「LINE受信」参照）
- [ ] QRコード・友だち追加リンクを社内クローザー・代理店（ピタサチ・wanny等）に共有する

## 1. 全体フロー

```
商談実施（クローザーが PLAUD NOTE で録音。ボタン1つ）
  → 商談後、LINEで「ユメイク営業分析bot」にそのままMP3/録音ファイルを送信
  → Botが受信した瞬間、内容に関わらず即座にSupabase Storageへ保存
    （LINE側の保持期限に依存しないようにするため。分析するかどうかはまだ決めない）
  → Bot「アポインターは誰ですか？」 → ボタンで選択
  → Bot「お客様の苗字を教えてください（例: 杉浦）」 → テキストで返信
    → システムが自動で「杉浦邸」の形に変換する
  → Bot「商談の結果は？」 → ボタン（契約/保留/失注/クーリングオフ/審査落ち/キャンセル）
  → Bot「この商談を分析しますか？」 → ボタン [分析する]/[分析しない]
      - [分析する]  → status='ready'（日次Routineの対象になる）
      - [分析しない] → status='skipped'（記録は残るが対象外。管理者が後から上書き可能）
  → 営業マンの作業はここまで（すべてタップ・一言返信のみ）
  ---
  → 毎日1回、日次Routineが自動で status='ready' の録音を確認し、たまっていればその場で処理
    （今川さんの「商談録音を分析して」の指示忘れで放置される事態を防ぐため。手動での即時実行も引き続き可能）
  → スキル 商談録音分析 が起動:
      Supabase（status='ready'の録音一覧）を確認
      → 録音をGeminiに丸ごと1リクエストで投入
      → Notion「DB 商談分析＆アポ分析」にページ作成
        （分析文＋クローザー/アポインター/結果/商材/採点等のプロパティ）
      → 分析対象があったクローザーに、その回の分析結果（総合点＋Notionリンク）とゴールマップ画像・進捗コメントを
        1人1通にまとめてLINEで通知（送信数を増やさないため）
      → 失敗した録音があれば、管理者（今川さん）にLINEで通知
  → 週次レビューでNotionのダッシュボード・データを横断確認
    → 共通課題を マニュアル/訪問営業.md・KPI.md に反映
```

管理者（今川さん）は、[分析しない]を選んだ商談も含めて、いつでもClaude Codeに聞くだけで一任して分析に回せる（セクション3参照）。

## 2. PDCAの対応表

| フェーズ | 何をするか | どこに残るか |
|---|---|---|
| **Plan** | 前回分析の「次回商談への改善アクション」を確認して商談に臨む | Notionページの「■9. 次回商談への改善アクション」 |
| **Do** | 商談を実施し、PLAUD NOTEで録音、LINEでユメイク営業分析botに送信 | 録音データ（Supabase Storage。**分析後も削除せず、そのまま保持し続ける**） |
| **Check** | 録音を丸ごとGeminiで分析・採点・要因分類 | Notion「DB 商談分析＆アポ分析」の各ページ |
| **Act** | 個人の改善アクションを決める。週次で共通課題を型に反映 | 週次レビュー ＋ [マニュアル/訪問営業.md](./マニュアル/訪問営業.md) ＋ [KPI.md](./KPI.md) 振り返りログ |

個人のPDCAは**商談1件ごと**、チームのPDCAは**週次レビュー**で回す。

## 3. 取り込み・分析パイプライン

### LINE受信（「ユメイク営業分析bot」側。実装済み: [line-webhook/](./line-webhook/)）

- @124rnagjとは別の新規LINE公式アカウントを作成し、社内クローザー・代理店の営業マン全員が友だち追加する
- 友だち追加時は、あいさつメッセージで使い方を自動案内する（LINE Official Account Managerの「あいさつメッセージ」機能で今川さんが設定。コードでは制御できない）:
  ```
  「ユメイク営業分析bot」へようこそ！

  担当者確認のため、まずお名前（苗字）を教えてください（例: 杉浦）。
  ```
  （苗字を受け取った後、Bot側が続けて「クローザー（商談録音を送る）」「アポインター（週次の実績だけ受け取る）」を
  ボタンで確認する。以降はそれぞれの役割に応じたフローに分岐する。詳細は下記の各フロー参照）
- 初回メッセージ時、送信者のLINE IDが未登録であれば「お名前（苗字）を教えてください」と聞き返す。送られた苗字が
  `line-webhook/api/webhook.py`の`KNOWN_MEMBER_NAMES`（Notionの「アポインター」選択肢と同期させたリスト）に
  一致すれば、`closer_line_users.closer_name`が自動で確定し、その場でBotが使えるようになる（今川さんの手動登録は不要）。
  一致しない場合は「今川さんに連絡してください」と案内し、今川さんが手動で`closer_name`を設定する
  （新メンバーの可能性があるため、その際は`KNOWN_MEMBER_NAMES`にも追加する）
- 録音（音声/ファイルメッセージ）を受信したら、**内容判定より前に必ず即座に**Supabase Storage（バケット `deal-recordings`）へコピーを保存する（LINE側の保持期限に依存しないため）。同時にSupabaseテーブル `deal_recordings` に行を作成し、`closer_line_users` テーブル（送信者のLINE ID→クローザー名。苗字の自己申告で自動確定。一致しない場合のみ今川さんが手動で設定）で担当クローザーを判定する
- Botは次の順でクイックリプライを出し、聞き返す:
  1. 「アポインターは誰ですか？」→ 自由入力（Notion側の選択肢が25名あり、LINEのクイックリプライ上限13個を超えるためボタン化していない）
  2. 「お客様の苗字を教えてください（例: 杉浦）」→ テキスト返信 → システムが自動で「〇〇邸」形式に変換して保存（Notionの既存表記に合わせる）
  3. 「商談の結果は？」→ ボタン（契約/保留/失注/クーリングオフ/審査落ち/キャンセル）
  4. 「この商談を分析しますか？」→ ボタン [分析する]/[分析しない]
     - [分析する] → `status='ready'`（日次Routineの対象になる）
     - [分析しない] → `status='skipped'`（記録は残るが対象外。削除しない）
- セットアップ手順・運用方法は [line-webhook/README.md](./line-webhook/README.md) を参照（今川さん個人のVercelアカウントで運用）

### 管理者（今川さん）の一任による上書き

新しいUIは作らない。今川さんがClaude Codeで次のように聞けば運用できる:

- 「今週 “分析しない” にした商談を見せて」→ Claude Codeが `deal_recordings?status=eq.skipped` をSupabaseから照会して一覧表示
- 「〇〇の商談も分析して」→ 該当行を `status='ready'` に上書きしてから通常の分析フローに乗せる

### 命名規則（録音ファイルの呼び方）

チャットで取得した「お客様名（邸）」「結果」をもとに、以下の形式に統一する。Supabaseの`deal_recordings.file_label`・分析中の一時ファイル名・Notionページ本文すべてでこの1つの名前を使う（呼び方がバラバラにならないように）。

```
YYYY-MM-DD_クローザー名_お客様名_結果.mp3
例: 2026-07-22_今川_杉浦邸_保留.mp3
```

- `YYYY-MM-DD`: LINEで録音が届いた日（`received_at`の日付）
- `クローザー名`: `closer_line_users`から判定した送信者名
- `お客様名`（〇〇邸の形式） / `結果`: チャットの聞き返しで確定した値
- 同じ日に同じクローザー・同じお客様名の商談が複数あれば `_2` `_3` と連番を付ける（他のフォルダの命名規則と同じ考え方）

### 分析（Gemini API・日次自動実行）

- **実行頻度は毎日1回**、Claude Code CloudのRoutineが自動で`status='ready'`の録音を確認し、あればその場で処理する（今川さんの指示忘れで放置されるのを防ぐため）。理由: 実際の商談数が少ない（平日1日1件程度、土日はクローザー5人×平均2件）ため、毎日チェックしても負荷は小さい
- 今川さんが「商談録音を分析して」「〇〇の商談も分析して」等とClaude Codeに指示する手動実行も、日次自動実行とは別にいつでも可能。スキル本体は [.claude/skills/商談録音分析/SKILL.md](./.claude/skills/商談録音分析/SKILL.md)
- チェックの実行回数自体に追加費用はほぼ発生しない（費用は実際に分析した商談数に比例するため）。毎日チェックしても、対象0件の日はGemini APIを呼ばないのでコストは増えない
- **録音は分割しない**。商談は2〜3時間を通しての伏線回収（前半のヒアリング・雑談が終盤のクロージングで効く）があるため、**商談全体を1リクエストで分析**する（Geminiは1プロンプトあたり最大約9.5時間の音声を扱える）
- 音声をそのまま入力するので、発言内容に加えて**声のトーン・声量・話す速さ・間の取り方**も評価できる
- プロンプトの実体は [scripts/deal_analysis_prompt.txt](./scripts/deal_analysis_prompt.txt)。内容はNotion「商談分析AI」ページ（心理学・行動分析ベースの9セクション構成）に準拠させてある。評価軸を変えたら両方（Notion側のページとこのファイル）を更新する
- 「採点」は100点満点。構成力・ヒアリング力・信頼構築力・クロージング力・総合力の5項目×各20点で配点が決め打ちされている（■7セクション参照）。クローザーへのLINE通知には内訳を出さず、合計点のみ記載する
- 分析本体は [scripts/deal_analysis.py](./scripts/deal_analysis.py)。音声→Gemini分析→`{"content_markdown":..., "properties":{...}}`のJSONを出力するところまでを担当し、Notionへの書き込みはスキル側（`mcp__Notion__notion-create-pages`）が行う。`--appointer`はLINEで確定した値をそのまま渡す（Geminiの推測に頼らない）
- モデルは通常 `gemini-2.5-flash`（1商談 約¥60）。重要商談の深掘りは `--model gemini-2.5-pro`（約¥150）
- **APIキーは有料（課金有効）プロジェクトのものを使う**（無料枠は入力データがモデル改善に使われ得るため商談音声には使わない）
- **Gemini APIは開発者向けの従量課金サービスで、Google/Anthropicの各種「Proプラン」等の消費者向けサブスクリプションとは別会計。** 使った分だけ必ず別途課金される。心配であればGoogle Cloud（請求先アカウント）側で月間の予算アラートを設定しておくとよい

### 失敗時の通知

- Gemini分析やNotion登録が失敗した録音があれば、その場で管理者（今川さん）にLINEで「〇〇邸の分析に失敗しました。原因: 〜」と通知する
- Supabase側は `status='ready'` のまま残し、次回実行時に再試行できるようにする

### 書き込み先（Notion） — どのページに溜まるか

- **データベース名**: 「📝 DB 商談分析＆アポ分析」
- **場所**: Notionワークスペース内、「営業部」ページの配下
- **URL**: https://app.notion.com/p/aa496718e62f4a70818d953492cad435
- 1商談＝このデータベースの1ページとして登録される。ページタイトルは「お客様名（〇〇邸）」、アイコンは📊。既存の260件と同じ場所・同じ形式・同じダッシュボード（契約率・失注理由等のグラフ）にそのまま乗る
- 月次の傾向は同じNotion内の「　営業実績　記録」ページ配下にある「📈 商談分析レポート」（このデータベースを集計したレポートページ）で確認できる
- Gemini分析からベストエフォートで埋まるプロパティ: 商材・給湯器・エネルギー・決裁者・年齢層（世帯主）・訪販経験・課題関心事・商談相手・築年数・エリア・子どもの年齢・ネック保留理由・備考・採点
- パイプライン側（LINE受信・スキル実行時の引数）で確定して埋めるプロパティ: クローザー・アポインター・お客様名・結果・商談日時・分析済み
- 録音ファイル名（上記「命名規則」の`file_label`）は、既存のデータベーススキーマを変更しないよう**プロパティではなくページ本文の先頭**に記載する（分析文の冒頭に「録音: 2026-07-22_今川_杉浦邸_保留.mp3」のように残す）
- **設定しないプロパティ**: 売上金額・支払方法・入金状況・入金日・完工日・報酬反映月・支払い反映済み（契約後の事務フローで別途入力される後工程項目のため）

### データの取り扱い

- **録音・文字起こし全文はこのリポジトリに置かない**（サイズと個人情報のため）。Supabase StorageとNotionページのみで管理する
- **録音データは分析後も削除せず、Supabase Storageにそのまま保持し続ける**（保持期間を区切って整理する運用はしない）。保存容量が増え続ける点だけ留意する
- 分析結果に書いてよい顧客情報は**氏名・エリアまで**。住所・電話番号・支払い口座情報は書かない（CLAUDE.md ルール2、プロンプト側でも出力禁止を指示済み）
- お客様への「録音・AI分析している」旨の説明は、現時点では行わない運用とする

## 4. ツール比較と費用（2026-07時点の検討メモ）

「LINEでMP3を送るだけで、声・間まで含めて、2〜3時間一括で分析し、Notionに自動反映される」要件での比較。為替は1ドル≒150円で概算。

| 案 | 費用 | 声・間の分析 | 3時間一括 | Notion自動反映 |
|---|---|---|---|---|
| **Gemini 2.5 Flash（採用）** | 従量: **約¥60/商談**（3h）。月20〜40商談で約¥1,200〜¥2,400 | ○ 音声を直接分析 | ○ 最大約9.5h | ○（このパイプライン経由） |
| Gemini 2.5 Pro（重要商談用） | 従量: 約¥150/商談（3h）。月20〜40商談で約¥3,000〜¥6,000 | ○ | ○ | ○ |
| PLAUD Proプラン | 年¥16,800（月1,200分＝商談6〜9件分で上限） | × 文字起こし・汎用要約のみ | △ | × |
| OpenAI Whisper API | 約¥160/商談（$0.006/分） | × テキストのみ | × 25MB制限で分割必須 | × |
| 商談解析SaaS（MiiTel等） | 月¥5,980/ID〜（amptalkは要問い合わせ） | ○ 話速・被せ等の定量化は強い | △ 電話・Web会議中心 | △（連携要開発） |

**結論**: 追加のツール契約・PLAUD課金・Notion有料連携（サードパーティ）は不要。Gemini APIの従量課金のみ（月20〜40商談でも月数千円以内）。対象0件の日はAPIを呼ばないため、日次自動実行にしてもコストは増えない。
費用の根拠: [Gemini API料金表](https://ai.google.dev/gemini-api/docs/pricing)、[PLAUD料金プラン](https://jp.plaud.ai/blogs/news/membership-update-starter)。

## 5. 週次レビュー（チームのPDCA）

- **頻度**: 週1回（週次MTGに合わせる）。対象はその週にNotionへ登録された全ページ（日々の日次Routineで随時登録されたものをまとめて確認する）
- **ファイル**: [商談分析/_週次レビューtemplate.md](./商談分析/_週次レビューtemplate.md) をコピーし `商談分析/YYYY/週次レビュー_YYYY-Wnn.md` で作成（このリポジトリに残す。個別分析はNotionが正）
- **やること**:
  1. Notionのダッシュボード（クローザー別/アポインター別契約率など）と`ネック・保留理由`から、担当別・フェーズ別の傾向を確認する
  2. つまずきが集中しているフェーズと失注要因の傾向を特定する
  3. 共通課題への対策を決め、型を変える場合は [マニュアル/訪問営業.md](./マニュアル/訪問営業.md) を即改訂する
  4. 月次の学びは [KPI.md](./KPI.md) の振り返りログにも転記する
  5. 各人の「来週の重点アクション」を1〜2個に絞って決める（→ 次週の各商談のPlanになる）

## 6. 追加の自動化（クーリングオフ対策・失注理由アラート・週次リマインド）

商談分析で蓄積されるデータを使って、以下の3つを追加で自動化する。

### 6-1. 契約翌日フォローリマインド（クーリングオフ対策）

- **背景**: Notionの月次レポートで「契約の約3割がクーリングオフで解約」「契約後に湧いた不安を誰も潰していない」ことが判明済み。対策として翌日のサンキューコール標準化が挙がっている
- **トリガー**: LINEで「結果＝契約」が確定した時点（商談分析の日次Routineとは無関係。分析する/しないの判断を待たずに動く）
- **実行**: 契約日の翌朝、対象クローザー本人にLINEで「〇〇邸へのお礼・フォロー連絡はお済みですか？」と自動送信
- **実装場所**: elp-goals側。日報リマインド等で**既に動いている日次Vercel Cronの仕組みに1つ処理を追加**する形にする（新しい仕組みをゼロから作らない）

### 6-2. 失注理由の急増アラート

- **タイミング**: 商談分析（Claude Codeのスキル実行）の直後（日次Routineによる自動実行・今川さんの手動実行どちらでも）
- **ロジック**: **直近7日間**にNotionへ登録された商談の「ネック・保留理由」を集計し、**同じ理由が2件以上重なったらフラグを立てる**（日次実行に切り替えたことで1回あたりの母数がさらに少なくなるため、「その回の実行分」ではなく直近7日間のローリング窓で見る。統計的な判定ではなくシンプルな閾値のまま）
- **通知**: 管理者（今川さん）へLINEで「直近7日間で、失注理由に“〇〇”が複数件見られました」
- **実装場所**: `.claude/skills/商談録音分析/SKILL.md`（分析の最後のステップとして追加）

### 6-3. 週次レビュー準備リマインド

- **タイミング**: **火曜**（週次MTGは水曜のため、前日に通知）
- **実行**: 今川さんへLINEで「今週の商談分析、週次レビューの準備はいかがですか？」と自動送信
- **実装場所**: elp-goals側。6-1と同じ日次Cronの仕組みに追加する

### 6-4. クーリングオフ発生の検知・優先分析アラート

- **背景**: 6-1（契約翌日フォロー）が予防策なのに対し、こちらは**実際にクーリングオフが発生した後**の早期把握が目的
- **検知元**: LINEの聞き返し（録音提出時点）ではなく、**Notion「DB 商談分析＆アポ分析」側で後から`結果`がクーリングオフに更新されたタイミング**を拾う（提出時点ではまだ契約として記録されていることが多いため）
- **仕組み**: Claude Code側の**日次Routine**（1日1回）が、Notionを`結果=クーリングオフ`かつ`クーリングオフ通知済み=false`で検索する。該当があれば管理者（今川さん）にLINEで「⚠ クーリングオフが発生しました（お客様名・クローザー名・日付）。次回の分析実行時に優先処理してください」と通知し、Notion側の「クーリングオフ通知済み」を`true`に更新する（二重通知防止）
- **原因分析（追加。ユーザー指示）**: 検知した各案件について、Supabase `deal_recordings` から
  お客様名・クローザー名が一致する行（この契約が最初に録音提出された際の行。`storage_path`に
  元の契約時の録音がある）を探す。見つかれば録音をダウンロードし、
  `scripts/cooling_off_analysis.py`（`scripts/deal_analysis.py`とは別の、クーリングオフ原因の
  振り返り専用プロンプト）で「なぜ契約後に解約されたか・次回への対策」を分析する。結果は
  Notionの該当ページに追記し（`mcp__Notion__notion-update-page` の `insert_content`、末尾に
  「■クーリングオフ原因分析（自動生成）」見出しを付けて追加）、今川さんへのLINE通知にも
  1〜2行の要約（主要因＋対策1点）を添える。該当する録音が見つからない場合はこの原因分析を
  省略し、通常の検知アラートのみ送る（分析対象外にしていた商談・音声削除済み等のケースがあるため）
- **なぜVercel側ではなくClaude Code側か**: Notionの検索にはNotion MCP（Claude Codeが使っている接続）が必要で、Vercel（LINE Webhook）側からは扱えないため
- **前提**: このRoutineがLINE pushを送るには`DEAL_LINE_CHANNEL_ACCESS_TOKEN`が必要。Routineが動く環境の環境変数として登録しておく（ローカルの`.env`は見えないため）
- **コスト**: 今川さん個人のClaude Proプランでの日次実行。1回あたり軽いNotion検索＋（該当時のみ）LINE push 1件のみのため、消費は小さい想定

### 6-5. 保留→契約／保留→失注の転換検知・要因分析アラート

- **背景**: 保留となった案件が、後日の追客（再訪問・電話。録音はない）を経て最終的に契約
  または失注に至ることがある。その転換の要因（何が決め手になったか／何が解消されなかった
  か）を、保留時点の商談録音を振り返ることで把握し、追客・次回商談に活かしたい（ユーザー
  指示）。実運用では保留→失注への書き換えはほぼ行われず、主に**保留→契約**のケースが対象
  （仕組み自体は両方に対応する）
- **記録の実態**: 保留→契約／失注の転換は、新しい録音・新しいNotion行が作られるわけでは
  なく、**Notion「DB 商談分析＆アポ分析」の同じ行の`結果`プロパティが手動で書き換えられる**
  （今川さん確認済み）。そのため、書き換わった後は「元々保留だった」という事実がその行から
  分からなくなる
- **検知の仕組み**: この問題に対応するため、ページ作成時点（`結果=保留`で登録された瞬間）に
  `初回登録時_保留`（チェックボックス）を`true`でセットしておく（`scripts/deal_analysis.py`
  が自動セット）。これにより、後から`結果`が書き換わっても「元は保留だった」ことが追跡できる
- **経過日数の記録（ユーザー指示。必ず記録する）**: 保留から何日後に結果が動いたかは音声から
  分からない情報のため、Notionの日付から機械的に計算する。「保留となった商談日」＝`商談日時`
  プロパティを使う。「結果が切り替わった日」は**Notionページの`page_last_edited_at`（最終更新
  日時）ではなく、Notion「📉 営業部 実績DB」の日次報告データ（対象クローザーの行で`契約`／
  `失注`列が最初に1以上になった`実績日`）から求める**（ユーザー指示・実例で修正）。理由:
  今川さんはLINEで商談結果の報告が上がった後、Notionの`結果`プロパティを手動でコピペ反映する
  運用のため、ページの更新日時（＝Notionを編集した日）と実際の商談報告日（＝実績DBに`/apohoukoku`
  等で反映される日）がズレることが実例（川村邸: ページ更新日時ベースでは2日と誤算出、実績DB
  ベースでは正しくは翌日=1日だった）で確認された。実績DBに該当日がまだ反映されていない場合の
  みフォールバックとして`page_last_edited_at`を代用し、その場合はNotion追記・LINE通知に
  「概算です」と明記する。算出した経過日数は「■0. 保留→結果の経緯」として
  Notionページの追記内容・LINE通知の両方に必ず含める。これは**元の商談録音が見つからず要因
  分析ができない場合でも記録する**
- **Claude Code側の日次Routine**が、Notionを`初回登録時_保留=true` かつ `結果 IN (契約, 失注)`
  かつ `保留転換分析済み=false` で検索する。該当があれば:
  1. 実績DBの日次報告データ（対象クローザー・`商談日時`以降で`契約`/`失注`が最初に1以上になっ
     た日）と`商談日時`から経過日数を算出する（上記。実績DB未反映時のみNotionページの
     `page_last_edited_at`にフォールバック）
  2. Supabase `deal_recordings` からお客様名・クローザー名が一致する行（保留時点の商談録音）
     を探す
  3. 見つかれば録音をダウンロードし、`scripts/pending_transition_analysis.py`
     （`--final-result 契約`または`失注`、`--pending-date`・`--transition-date`で経過日数も
     渡す）で「保留から契約／失注に転じた要因と対策」を分析する。見つからない場合は要因分析
     を省略し、経過日数の記録のみ行う
  4. 結果（経緯＋要因分析、または経緯のみ）をNotionの該当ページに追記する（`insert_content`、
     末尾に「■保留転換分析（自動生成）」見出しを付けて追加）
  5. **要因分析ができた場合のみ**、担当クローザー本人にもLINEで「〇〇邸の商談が保留→契約に
     なったため、商談を分析し直しました。」＋Notionリンクを送る（`closer_line_users`から
     `closer_name`で本人の`line_user_id`を引く。該当がなければこの個人通知は省略し、今川さん
     への通知は通常通り行う）（ユーザー指示）
  6. 今川さんへLINEで「〇〇邸が保留→契約になりました（保留から約n日）。要因: 〜（詳細はNotion
     参照）」と通知し、Notion側の`保留転換分析済み`を`true`に更新する（二重通知防止。このpush
     の成否を基準にフラグを更新する。担当クローザーへの個人通知が省略された場合でも問題ない）
- **前提・コスト**: 6-4と同様（`DEAL_LINE_CHANNEL_ACCESS_TOKEN`が必要。今川さん個人のClaude
  Proプランでの日次実行、消費は小さい想定）

### 6-6. 週次目標配信（水曜8:00の自動送信）＋リッチメニューでの都度確認

- **背景**: 日次の自動プッシュ通知は配信数上限のリスクが高いため廃止済み。**水曜8:00
  JSTの週1回だけ自動送信し、それ以外は本人がリッチメニュー「📊 今日の目標を見る」を
  タップした時だけ返信する**方式に統一する（ユーザー指示）
- **データソース**: Notion「📊 個人別KPI逆算データ（参照用）」DB
  （`collection://24592ef5-57e2-42d7-93a1-4686d2d7f983`）の数式（formula）プロパティを、
  計算し直さずそのまま使う:
  - `通知本文（LINE配信用）` — 曜日に応じて「本日の目標」（平日）／「今週任された数字」
    （土日）に自動で切り替わる。リッチメニュー返信で使う
  - `_週末メッセージ_クローザー` / `_週末メッセージ_アポインター` — 曜日に関係なく常に
    「今週任された数字」の内容。水曜8:00の自動送信はこちらを直接使う（`通知本文`は
    平日扱いになり週次内容にならないため使わない）
  - `在籍状況(名簿参照)`（営業メンバー名簿DBからのロールアップ）で退職者を除外する
- **⚠ 数式プロパティの取得はNotion公式REST APIが必須**: Claude Codeが使っているNotion MCP
  接続では、数式・ロールアップ型プロパティの計算結果を読み出せない（参照用のURLしか
  返らず、中身の文字列が取得できない。実際に確認済み）。そのため本番のLINE Webhook
  （Vercel）・週次送信スクリプトのどちらも、`tools/roadmap/sync_notion_to_json.py`と
  同じ方式でNotion公式REST APIを直接叩く（`NOTION_TOKEN`が必要）
  - セットアップ（初回のみ・Notion UI操作）:
    1. https://www.notion.so/my-integrations でInternal Integrationを作成する
       （`tools/roadmap`用に既にトークンがあれば使い回してよい）
    2. Notionで「📊 個人別KPI逆算データ（参照用）」DBを開き、右上「•••」→
       「Connections」→ 作成したインテグレーションを追加（共有）する
    3. 発行されたInternal Integration Secretを、**このリポジトリやチャットには書かず**、
       (a) Claude Codeが動く環境（このRoutineを実行するセッション）の環境変数、
       (b) VercelのlineWebhookプロジェクト（line-webhook）の環境変数、
       の両方に`NOTION_TOKEN`として設定する
- **対応1: 水曜8:00の自動送信（`scripts/weekly_goal_push.py`）**
  - 在籍中・役割（クローザー／アポインター）ごとに`_週末メッセージ_*`を取得し、
    Supabase `closer_line_users`の`closer_name`と突き合わせてLINE Push APIで1人1通送る
  - 送信失敗は3回までリトライし、それでも失敗した担当者・スキップした担当者
    （LINE user_id未登録・本文未算出等）があれば、今川さんへ1通でまとめて通知する
  - 実行: `python3 scripts/weekly_goal_push.py`（`--dry-run`で送信せず内容確認できる）
  - 新規Routine（水曜8:00 JST = 火曜23:00 UTC、cron `0 23 * * 2`）から呼び出す想定。
    **`NOTION_TOKEN`の動作確認ができるまでは作成しない**
- **対応2: リッチメニュー「今日の目標を見る」（`line-webhook/api/webhook.py`）**
  - `handle_goal_request`が、タップ＝固定テキスト「今日の目標を見る」の受信をトリガーに、
    送信者の`closer_name`で「📊 個人別KPI逆算データ（参照用）」を検索し、`通知本文（LINE
    配信用）`をその場でReply API（`replyToken`使用。Push APIは使わない）で返す
  - **配信数カウントの確認結果**: LINE公式ドキュメント（[LINE Developers
    「今さら聞けない！Messaging APIの通数カウント方法」](https://developers.line.biz/ja/tips/2026/05/28/how-to-count-messages/)）
    により、**Reply API（応答メッセージ）は現行のどのプランでもメッセージ配信数に
    カウントされない**ことを確認済み。そのため既定（`REPLY_COUNTS_TOWARD_QUOTA=false`）
    では連打時も毎回Notionから最新値を取り直して返す。5秒以内の連打・LINE側の再送は
    別途デバウンスする（Supabaseの`last_goal_reply_at`列。未追加でも安全にスキップされる）
  - 将来の仕様変更等でカウント対象になった場合のみ、Vercelの環境変数で
    `REPLY_COUNTS_TOWARD_QUOTA=true`にする。その場合は1ユーザー1日1回（JST日付基準）に
    制限し、同日2回目以降のタップには直近の内容を再送する（`last_goal_reply_date`・
    `last_goal_reply_text`列を使う）
  - **Supabaseマイグレーション**: `line-webhook/supabase/schema.sql`末尾の
    `last_goal_reply_date`・`last_goal_reply_text`・`last_goal_reply_at`の3列を追加する
    `alter table`をSQL Editorで実行する（未実行でも本文の返信自体は動く。カウント・
    連打防止の付随機能だけが無効化される安全な設計）
- **対応3: リッチメニュー画像（`scripts/deal_bot_richmenu.py`）**
  - デザインは気合い・勢いを感じるファイヤー（爆発）モチーフ、イメージカラー赤、
    グロー演出（ユーザー指示）。numpyの乱流ノイズで放射状の爆発背景を生成し、中央に
    グロス調の円形ボタン（無地）を重ねる。フォントはNoto Sans CJK Bold（インストール
    済みの場合）を優先し、なければIPAGothicにフォールバックする
  - `--image-only`で画像生成だけ確認できる（LINEへの登録なし）
  - **役割で内容を出し分ける（ユーザー指示）**: リッチメニューを2種類作成する
    - 全員向けデフォルト（`user/all/richmenu`）: 「今日の目標を見る」1ボタン
    - クローザー専用（`営業分析BOT クローザー用`という固定名で作成。IDでは持たず
      名前で引く。デザインを作り直してIDが変わっても`line-webhook/api/webhook.py`
      側の参照が追従できるようにするため）: 左右2分割で「今日の目標を見る」＋
      「直近の商談分析結果を見る」の2ボタン。アポインターは商談分析の対象外なので
      デフォルトのままでよく、専用メニューは作らない
    - `line-webhook/api/webhook.py`の`link_closer_richmenu`が、`closer_line_users.role`
      が`closer`に確定したタイミング（自動一致登録・自己申告登録の両方）で
      `linkRichMenuToUser`を呼び、クローザー専用メニューをその人だけに個別リンクする
      （メニュー未作成時は何もせず、登録フロー自体は失敗させない）
    - `python3 scripts/deal_bot_richmenu.py --backfill`で、既に登録済みの
      クローザー全員にも一括でクローザー専用メニューをリンクできる（新規登録時の
      自動リンクとは別に、導入時・デザイン変更時に1回実行する）
  - **「直近の商談分析結果を見る」ボタン**: `handle_analysis_request`が、Notion
    「DB 商談分析＆アポ分析（契約案件一覧）」（database_id:
    `aa496718-e62f-4a70-818d-953492cad435`。**要Notionインテグレーション共有**
    ＝KPI DBとは別に「•••」→「Connections」で追加が必要）から、送信者の
    `closer_name`と一致する`クローザー`列の行を`商談日時`降順で1件検索し、
    お客様名・結果・採点・NotionページURLをReply APIで返す。役割ガードとして
    `role != "closer"`の場合はサーバー側でも断る（専用メニューからしか出ないボタン
    だが、念のため）
- **既存「週次実績配信」スキルとの関係（ユーザー指示: 新方式に完全移行）**: 既存の
  `.claude/skills/週次実績配信/SKILL.md`（毎週水曜9:00 JST自動実行、Routine
  `trig_01BGQHi3oaHVP89t33RUejsa`）は、実績DB等から進捗バー・チーム予算・失注理由
  アラート・今川さん向け管理者サマリーを**自前計算**して配信していた。今回の新方式は
  同じ曜日・近い時間帯に、Notion数式で計算済みのテキストをそのまま送るだけのため、
  **新方式の動作確認が取れ次第、この自動Routine（水曜9:00）は無効化する**（スキル自体
  ・手動での「週次配信して」実行は当面残すが、自動実行は新方式に一本化する）。
  失注理由アラート・今川さん向け管理者サマリーを新方式でも引き続き出したい場合は
  別途相談する（現時点の新方式には含まれていない）
- **ロールアウト手順（実施順）**:
  1. 上記の`NOTION_TOKEN`セットアップ（今川さん作業。KPI DB・商談分析＆アポ分析DBの
     両方をNotionインテグレーションに共有する）
  2. `python3 scripts/weekly_goal_push.py --dry-run`で全員分の本文が正しく取得できるか確認
  3. Vercel（line-webhook）に`NOTION_TOKEN`を環境変数として追加し、再デプロイ
  4. `python3 scripts/deal_bot_richmenu.py --backfill`でリッチメニュー2種類を登録し、
     既存クローザー全員にクローザー専用メニューをバックフィルする
  5. リッチメニューをタップして返信を確認 → 問題なければ`weekly_goal_push.py`を
     呼ぶ新規Routine（水曜8:00 JST）を作成し、既存の週次実績配信Routine（水曜9:00）を
     無効化する

### 6-7. 平日メッセージの内容調整（訪問数上限・進捗表示、ユーザー指示）

- **`今日やるべき訪問数`の1日上限を平日80件・土日100件に変更**（従来は一律50件）。
  `min(if(day(today())==0 or day(today())==6, 100, 80), ...)` で曜日判定する
  （`day()`は0=日曜〜6=土曜）。「週1契約ペース」を少なすぎる目標にしないための対応
  （ユーザー指示）。ただし`逆算モード`（率改善優先／2週で1契約に切り替える閾値）は
  まだ旧来の「1日50件」基準のまま据え置いている。上限を上げたことで、本来なら
  週1契約ペースを維持できるはずの人が閾値の古い基準のせいで「2週で1契約」等に
  格下げされているケースがないか、次に相談があれば`逆算モード`の閾値も合わせて
  見直す
- **`逆算ロジック（表示用）`に改行を追加**（3モード＝週1契約／2週で1契約／率改善優先
  すべて）。スマホで見たときに1文で詰め込まれず読みやすいように、見出し・根拠・
  結論を改行で区切る形に統一した
- **新規プロパティ`_進捗表示`を追加**し、`_平日メッセージ`（逆算ロジックの直後）に
  組み込んだ。週・月それぞれの「目標件数中の実績件数・達成率%」を機械的に算出する:
  - 訪問: 週（`今週の必要訪問数`目標 × `今週実績訪問数`実績）／月（`今月訪問実績`実績 ×
    `今月訪問実績+今月残り訪問数概算`で逆算した月目標）。全員に表示
  - アポ: 週は全員表示。**月は`役割`が`アポインター`の場合のみ表示**
    （`今月アポ目標概算`はアポインター向けの値で、クローザーは常に0のため、
    表示すると「X/0件(0%)」という壊れた見た目になる。クローザーは月間アポ目標を
    追わない設計のため意図的に非表示）
  - 契約: 月（実績 × 目標）を**全員**に表示。クローザーは`今月契約実績`・`今月契約目標`の
    生値をそのまま使う。**アポインターは`今月契約目標`が空なので、目標だけ
    `今月予算万円 ÷ 65`（1件あたりの契約単価を60〜70万円と仮定し、その中間の65万円で
    概算。ユーザー指示。最低1件に切り上げ）で算出する**。実績は引き続き`今月契約実績`
    （アポインターは未入力＝0のまま。個人単位でアポ経由の契約を数える仕組みが無いため、
    データが埋まるまでは「0/n件」という進捗の少ない表示になる。将来的に実績側を
    埋めたい場合は別途相談）
  - **売上と、契約の週次実績は今回は追加していない**（ユーザー指示: 訪問・アポ・契約・
    売上の4項目を週/月で出したいという要望だったが、以下の理由でデータが無く保留中）:
    - `📊 個人別KPI逆算データ（参照用）`DBには「今週実績契約数」のような週次契約実績の
      生プロパティが存在しない（`今週実績訪問数`・`今週実績アポ数`にあたる列が契約には
      ない）。誰が・どうやってこのDBの他の「実績」列（`今月訪問実績`等）を日々更新して
      いるのか把握できていないため、同じ仕組みに乗せて追加してよいか要確認
    - 売上の実績（週・月とも）は、このDB自体にプロパティが存在しない。月間の目標だけは
      `今月予算万円`として既にあるが、実績側は「📉 営業部 実績DB」の契約金額列を都度
      集計しないと出せず、Notion数式だけでは（このDBから直接見えるリレーションがないため）
      計算できない。`週次実績配信`スキル（旧方式）は実績DBを直接SQLクエリして計算して
      いたが、新方式（Notion数式ベース）でこれを再現するには、実績DBまたは
      `予算と達成率`DBとのリレーション＋ロールアップを新設する必要がある
    - **ユーザー判断（確定）**: 「一旦よし」＝ (b) を採用。契約の週次実績・売上（週/月）は
      現時点では追加しない。実績DBとのリレーション新設は保留のまま
    - **アポインターの契約(月)目標について（確定）**: 上記の通り`今月予算万円 ÷ 65`で
      算出する方式が確定。実績（`今月契約実績`）は未入力のままのため、目標に対して
      常に0からのスタートに見える点は既知の制約として残る
- **わかった開発上の事実**: これまで「Notion数式（formula型プロパティ）はUIの数式
  エディタでしか編集できず、Claude Codeからは中身のテキストを取得して手動貼り付けを
  お願いするしかない」という前提で運用してきたが、今回`mcp__Notion__notion-update-data-source`の
  `ALTER COLUMN "プロパティ名" SET FORMULA('...')` DDLで実際に数式の中身を書き換えられる
  ことを確認した（Notion REST APIでの読み出し後の`expression`が実際に更新されていることも
  確認済み）。**今後の数式プロパティの追加・修正は、都度Notion公式REST APIで最新の数式
  ソースを取得したうえで、このMCPツールで直接書き換えてよい**（ユーザーにUIでの
  手動貼り付けをお願いする必要はなくなった）。ただし数式の**計算結果の読み出し**
  （fetch/queryツールでの値取得）は引き続きNotion MCPでは不可で、Notion公式REST APIが
  必須という制約は変わらない（上記「⚠」の注記の通り）

### 6-8. 前回共有した内容の再認識メッセージ（ユーザー指示）

- **背景**: 営業マンはLINEの日報コマンド（`/shoudannhoukoku`等、`ユメイク営業分析bot`とは
  別のLINE Botが担当）で日々の数字を報告する際、自由記述の一言も一緒に送っており、これが
  「📉 営業部 実績DB」（`collection://f11afda4-a39d-4139-8e12-817d3f70267b`）の`備考`列
  （rich_text）にそのまま反映される（ユーザー確認済み）
- **実装**: `line-webhook/api/webhook.py`に`notion_query_latest_remark`を追加。
  平日のリッチメニュータップ（`handle_goal_request`）時のみ、実績DBから「本人の、
  タップ日より前で直近の`実績日`行にある非空の`備考`」を検索し、見つかれば通知本文の
  最上部（⚔️営業ステータスの直後）に「📝 前回共有した内容\n<備考>\n\n」を追加する。
  **「前日」固定ではなく、間が空いていても直近の報告内容をそのまま出す**（ユーザー指示。
  例: 水曜にタップしたら、直近の報告が日曜だった場合は日曜の内容が出る）。該当行が無い
  場合（報告が一度もない等）は何も追加しない。週末メッセージ（`_週末メッセージ_*`）には
  追加しない（週次の内容であり日々の報告内容と単位がそぐわないため）
- **⚠ 追加のNotion共有が必要**: この機能は「📉 営業部 実績DB」への問い合わせを新たに行う。
  `NOTION_TOKEN`の統合は現状このDBに共有されていない（未共有だと404になることを確認済み）。
  今川さんに、このDBも「•••」→「Connections」で同じ統合に共有してもらう必要がある
  （KPI DB・商談分析＆アポ分析DBとは別に、計3つのDB共有が必要ということになる）

### 6-9. ⚔️ 営業ステータス（ゲーム性、ユーザー指示）

- **背景**: 営業マン目線でBotにゲーム性を持たせたい、というユーザー要望。「★を使った
  5項目のステータス表示」という具体案が出た（例:「クロージング★5」「アポ力★4まで上げたい」
  等、本人が自分の強み・伸びしろを一言で語れるようにする狙い）
- **算出方法（ユーザーが各項目の見方を指定。当初案から変更済み）**: `line-webhook/api/webhook.py`の
  `build_status_block`が、KPI DBの1行（`page`）と「📉 営業部 実績DB」の直近実績行
  （`recent_rows`、最大30日分）から5項目を算出する。メッセージの**最上部**
  （`前回共有した内容`よりも上）に「⚔️ 営業ステータス」として表示する。平日・週末どちらの
  タップでも表示する（`前回共有した内容`とは異なり曜日を問わない）:
  - **行動力**: 直近の実績行1件の活動量。**クローザーは`商談数`、それ以外（アポインター）は
    `訪問数`**（ユーザー指示:「毎日の訪問数、クローザーだったら商談数で見てほしい」）
  - **アポ力**: 直近実績（最大30日）の中で最も多かった1日の`アポ数`（ユーザー指示:
    「1日の最大アポ数で見る」＝平均ではなくピーク能力を見る）
  - **商談力**: KPI DBの`有効商談化率%(アポ→有効商談)`の実数（ユーザー指示:
    「アポから商談に繋がった件数の商談作成率」に対応する既存の実測値をそのまま使う）
  - **クロージング**: KPI DBの`採用_契約率%`の実数（ユーザー指示通り契約率をそのまま使う）
  - **継続力**: 行動力と同じ活動量指標（訪問数/商談数）について、直近の出勤日における
    「波の無さ」（ユーザー指示:「結果や行動面で波があるかないかで判断」）。変動係数
    （標準偏差 ÷ 平均）が小さいほど「波が無い」として高評価にする。データが3件未満の
    場合は中間評価にする（新規登録者を不当に低評価しないため）
  - 各項目とも0〜5つ星（`⭐`/`☆`）で表示し、最低保証は1つ星（0つ星は表示しない。
    やる気を削がないため）。「出勤状況」が休みの行は行動力・継続力の計算対象から除外する
- **⚠ 閾値は初期の目安値**: 行動力（当日の訪問数/商談数の絶対値）・アポ力（ピークのアポ数）・
  商談力/クロージング（率の絶対値）・継続力（変動係数）いずれも、観測データのばらつきを
  踏まえた初期の目安値で`stars_text`にbandsとして渡している。運用してみて「全員5つ星に
  張り付く」「誰も3つ星を超えない」等の偏りが出た場合は、この閾値を調整する（ユーザーの
  実感と星の数が一致するよう、実データを見ながら数回チューニングする前提）
- **ユーザー例示との差分**: ユーザーが挙げた「7日連続で目標達成→継続力+1」のような
  文字通りの連続日数（ストリーク）判定は、過去の各日の目標値を遡って再計算する必要があり
  現状のデータ構造では実現できないため採用していない（当日その場で計算した「今日の目標」
  は保存されておらず、後から「その日は目標を達成していたか」を判定できない）。代わりに
  「直近の活動量の変動係数（波の無さ）」を継続力の指標として採用した

## 7. Claude Code への指示例

- 「商談録音を分析して」→ スキル `商談録音分析` が `status='ready'` の録音をまとめてNotionに反映（毎日、日次Routineでも自動実行される。手動でいつでも即時実行も可）
- 「今週 “分析しない” にした商談を見せて」→ `skipped`の一覧を表示（管理者の一任判断用）
- 「〇〇の商談も分析して」→ `skipped`だった商談を`ready`に上書きして分析
- 「今週の商談分析から週次レビューを作って」→ Notionのデータを参照して週次テンプレで横断集計
- 「〇〇さんの直近5件の商談の傾向と改善点をまとめて」→ NotionのDBを検索
- 「失注理由を今月分で集計して」
- 「週次レビューの決定を訪問営業マニュアルに反映して」
